# Dependency version constraints for reproducible builds
# This file pins all dependencies to specific versions for consistency
# across development, testing, and production environments.
#
# Usage:
#   pip install -r requirements.txt -c constraints.txt
#
# To update:
#   pip-compile requirements.txt --output-file=constraints.txt
#   or manually update versions after testing
#
# Note: Constraints cannot include extras like [standard].
# The extras are specified in requirements files, not constraints.

# Core API dependencies
fastapi==0.115.6
uvicorn==0.32.1
httpx==0.27.2
python-dateutil==2.9.0

# Security dependencies
slowapi==0.1.9

# Development dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.3.0
black==24.10.0
ruff==0.8.4
pre-commit==4.0.1

# Type checking
mypy==1.13.0

# Security scanning
pip-audit==2.7.3
